# Changelog
All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Changed
- Important: OnlineSampler and children classes (IntervalsSampler, RandomPositionsSampler) are now initialized with
 `target` argument instead of `target_path`. `target` now supports both str input like `target_path` and a `Target`
 object.

## [0.4.8]
Changes before 0.4.8 are not included in this log.
